<script lang="ts">
  import Token from "./Token.svelte";
</script>

<div class="grid grid-cols-6 gap-3">
  <!-- <Token
      tokenId={token.id}
      value={token.value}
      selectable={$service.matches("SelectFirstToken") ||
        $service.matches("SelectSecondToken")}
      useIcon={$service.context.icons}
    /> -->
</div>
